# Changelog

## 1.13.7

### 变更

- **不再单独提供 exe 安装文件**：每个版本只发布压缩包 `PaperFlow-x.y.z-win-x64.zip`（另外附源码包、`update.json` 与构建信息）。程序内的自动更新改为下载这个压缩包，从里面取出 `versions/<版本>/PaperFlow.exe`，校验两层哈希（压缩包一层、里面的程序一层）之后再安装。
- 更新清单新增两个字段：`exeSha256` 与 `exeLength`，描述压缩包里那个程序文件；`sha256`/`length` 描述压缩包本身。旧格式（清单直接指向 exe）仍然读得懂。
- README 重写成"先教下载"：GitHub 与 Gitee 各四条点击步骤，后面才是用法与界面图。
- 程序"关于"页只写"只在检查更新时联网，只下载、不上传"，不再点名某个站点。

### 兼容性

- **1.13.6 及更早的版本不会自动看到 1.13.7**：它们只认清单里以 `.exe` 结尾的地址，而新清单指向压缩包，所以它们的检查会按"清单无效"处理，提示"检查更新没成功"，不会安装任何东西。这些机器需要手动下载一次 1.13.7 的压缩包；从 1.13.7 起，之后的版本都能自动更新。
- 已发布的旧版本 Release 里的 exe 附件已删除，只保留压缩包。

### 新增检查

- 新增 8 项（共 363）：压缩包更新会解出里面的程序、解压后压缩包本体被清理、包里缺文件或里面的程序被换过一律拒收、清单缺少 `exeSha256` 时拒收、旧格式清单仍等价、`channel.json` 记录的是程序而不是压缩包。

## 1.13.6

- 更新来源之间加了一条更严的规矩，防止"镜像被动手脚"：**镜像不许抢先**——如果 Gitee 报的版本比 GitHub 高，直接忽略它（镜像只允许落后，不允许领头）；**同一个版本必须两份清单的 SHA-256 与长度完全一致**才安装，对不上就这次先不更新（提示"两个更新来源对同一个版本给的文件不一样"）；只有 GitHub 连不上时才会单凭镜像更新（国内的常态，写在文档里）。
- 镜像脚本进了仓库：`scripts/Mirror-Gitee.ps1`（通用版，默认以脚本所在的仓库为源，不含任何私人路径）。发布脚本新增 `-MirrorToGitee -GiteeOwner <用户名>`，一条命令就能"构建 + 部署自用 + 发布到 Gitee"；同时会在本地打上 `v<版本>` 标签，保证 GitHub 和 Gitee 用的是同一个标签对象。
- 新增 4 项检查（共 355）：镜像报更高版本时被忽略、同版本同哈希放行（并走镜像下载）、同版本不同哈希拒收、只剩镜像时仍能更新。

## 1.13.5

- 程序内自动更新现在有**两个候选清单地址**：先读国内镜像 Gitee，再读 GitHub——而且是**两边都读、取版本更新的那一份**（不是"谁先通用谁"），所以镜像落后一两步也不会漏掉更新。两个都读不到才报错，提示也改成"连不上更新服务器（Gitee 和 GitHub 都没连上）"，不再只提 GitHub。
- Gitee 镜像上线：https://gitee.com/pan-wang-yuang/paperflow ——源码、分支、标签和当前版本的发行版都在；另外维护一个固定的 `latest` 发行版，里面只有一份 `update.json`，指向 Gitee 自己的下载地址（国内的更新下载不再绕回 GitHub）。
- 新增 `工具脚本\Mirror-Gitee.ps1`：一条命令完成"建仓库 → 推分支与标签 → 建版本发行版 + latest 发行版 → 校验镜像出的清单与本地一致、下载地址可访问"；重复跑是幂等的，令牌不写进 `.git/config`。
- 设置里的说明改成"检查更新只读一份静态清单（国内镜像 Gitee 优先，其次是 GitHub）……"。
- 新增 5 项检查（共 351）：镜像优先但取更新的那一份、镜像挂了能回退上游、两边都挂要报成网络问题、两边都还是旧版时不提示、默认候选顺序是 Gitee 在前 GitHub 在后。

## 1.13.4

- **删掉 1.13.3 刚加的网络代理设置。** 理由：国内访问慢这件事应该由以后加 Gitee 镜像来解决，不该让用户在设置里填一个地址。代理相关的设置字段、界面、解析与检查全部移除；下载"不设总时长、90 秒收不到数据才算断"的行为保留，检查更新失败的提示也保留。
- **同步那一段重写，不再假定任何人都在用同步文件夹。** 用着就显示"正在使用同步文件夹"加地址和"打开同步资料文件夹"按钮；下面只有一句提示：“如果你使用 OneDrive / 百度网盘 / Dropbox / 坚果云 等同步文件夹，在同一个文件夹下，其他电脑收到后会同步你的设置哦。”原来那句"其他电脑收到文件后会自动刷新。字体、主题、字号……"删掉了。
- 检查项 346 项（去掉 6 项代理检查）。

## 1.13.3

- **检查更新失败不再沉默**：连不上、超时或清单读不出来时，窗口底部会出现一条可关掉的提示（每天最多一条，由三档节奏决定），写清"这次没连上 GitHub，没能检查更新"，并给一个"现在再试一次"按钮；设置页的"上次检查"也会改成"上次检查没成功（时间）：原因"，失败原因存在本机，下次打开还能看到，检查成功就自动清空。
- 失败原因翻成人话：连不上 / 超时 / 清单读不出来分别有各自的说法，不再是原始异常文本。
- "关于与反馈"页只显示版本号，不再显示 Git 提交号（提交号仍然在 `build-info.json` 与 Release 附件里，给维护者用；普通用户看到它只是噪音）。
- 删掉一处死代码：`Appearance.Map` 里两个永远匹配不上的暖色分支（判断 `Current.Name is "夜墨"`，但主题名实际是"经典 · 夜墨"，而且这两个颜色在别处根本没有调用）。
- 设置里"检查更新"的说明不再提同步文件夹，统一成"只读 GitHub 上的一份清单，只下载、不上传，论文数据不会被发送出去"。
- 新增 9 项检查（共 346）：网络类失败与清单类失败分得开、超时与套接字错误算网络问题、其它异常保留原文、失败原因能存下来、首次检查前没有检查时间。

## 1.13.2

- “关于与反馈”页改成不说同步文件夹：只写“不需要注册账号、论文数据只存在你自己的电脑上、只在检查更新时访问 GitHub，只下载不上传”。
- 同步文件夹只在 设置 → 同步与启动 里提，并且写清楚它是可选的：默认论文就在这台电脑上；想换电脑或几台电脑一起用，把安装文件夹放进一个会自动同步的网盘文件夹（例如 OneDrive），在每台电脑打开那里的固定启动入口，其他电脑收到文件后会自动刷新。不再假定用户一定用某个网盘。
- README 重写成用户视角：一小段说明 + 下载 + 几步使用 + 六张宣传图，细节交给图片；删掉了面向维护者的同步原理、数据边界表、发布与开发说明。
- 宣传图重新导出（`--promotional-assets`），六张 2880×1920，全部是代码生成的虚构论文，未读取任何真实资料。

## 1.13.1

- 修掉发布脚本拼更新清单地址的毛病：1.13.0 构建出的 `update.json` 里 `url` 少了附件名（`.../v1.13.0/`），程序内更新会找不到文件。这一版改用 `-f` 拼字符串，并加了四条断言——附件必须存在、清单里的版本/地址/哈希/大小必须和附件真身逐个对上，对不上就让你发不出包。1.13.0 从未公开发布，也不会成为自动更新的目标。

## 1.13.0

（1.13.0 没有公开发布：它构建出的更新清单地址是错的，程序内更新会找不到文件。公开发布的是 1.13.1，内容与下面这些相同。）

- 界面支持中文和英文：设置 → 外观 → 界面语言，可选跟随系统、中文、English，默认跟随系统。切换语言只改显示，**数据一个字不动**——阶段名、优先级、状态、翻页与排序方式、主题名在文件里仍是原来的规范值，所以老配置、老同步记录和不同语言的电脑之间完全兼容，不需要任何迁移。
- 改语言的界面重新打开后全部生效：保存设置时挂件会自动重启一次（约一秒），托盘的挂件窗口与菜单都是新语言。
- 程序内自动更新：设置 → 同步与启动 → 更新提示三档（每次都提示 / 每天一次 / 不提示，默认每天一次），旁边有“现在检查一次”。检查只读 GitHub Release 里的一份静态清单，是普通 GET，不带请求体、不带 Cookie、不带设备标识；选“不提示”则一次联网都不做。
- 发现新版后挂件底部出现一条提示：“下载并安装” → 后台下载并显示百分比 → 校验长度与 SHA-256 → 写入 `versions/<版本>/PaperFlow.exe` 并更新 `channel.json`（旧清单留 `.previous`）→ 提示“重启并更新”。校验不过的文件直接删掉，绝不会覆盖现在的程序；下载永远由你点按钮发起，不静默安装。
- 自动重启用 `--wait-for` 先等旧进程退出，避免单实例锁把挂件弄丢；更新后的重启直接打开新版本自己的 exe。
- 隐私说明重写：README 与 SECURITY 不再说“完全不联网”，改为写清“只在检查更新时访问 GitHub，只下载、不上传”；关于页同样改口径。
- 发布流程加两样附件：`PaperFlow-<版本>-win-x64.exe` 与 `update.json`，并断言清单里的哈希与构建产物一致。
- 开发与自动化验证用 `--update-url` 可以把清单指到本地服务器，整条更新链能在不联网的情况下端到端跑。
- 新增 48 项检查（共 337）：词表每条都真的换了语言、源码里没有漏翻译的中文、数据规范值与语言无关、修改记录按段翻译、单复数和分隔符、清单解析拒绝坏数据、三档检查节奏、下载校验、安装后的 `channel.json` 与启动器契约一致、下载是 GET 且无请求体无 Cookie。
- 开发过程中修掉一个真问题：自定义下拉框模板不认 `DisplayMemberPath`，绑对象的下拉在选中框里会显示成 `Choice { Value = ..., Label = ... }`；改成 `Choice.ToString()` 返回显示文字。

## 1.12.0

- 程序现在能自己放快捷方式：设置 → 同步与启动 里勾上“桌面上放一个入口”“开始菜单里放一个入口”，保存设置时生效，取消勾选就移除。托盘菜单也加了一项“放好桌面和开始菜单快捷方式”，一键把两个都做好。
- 快捷方式指向固定启动入口 `PaperFlow.Launcher.exe`，不是某个版本目录里的 exe，所以以后换了版本也不用重建；万一这台电脑上还没找到固定启动入口，会退回指向当前程序文件，并在设置里写明这一点。
- 只动自己做的那个文件：同一个位置若放着一个同名但指向别处的快捷方式，程序会拒绝删除并说明原因，而不是替用户做决定。
- 任务栏图标不能由程序自己钉（Windows 不允许），设置页和托盘提示里都写了“右键快捷方式选固定到任务栏”这一步。
- 设置 → 同步与启动 新增“打开程序文件夹”；“更新”一节改写成当前的同步文件夹方案（原来那句“排在 1.6.0”早已过期）。
- 关于页补上 GitHub issue 入口，并去掉“仓库还没公开”的过期说明。
- 新增 17 项检查：快捷方式创建、读取目标、删除、缺少启动入口时的回退、目标不是本程序时拒绝删除。

## 1.11.1

- “排序方式”不再是一重启就悄悄回到手动排序的临时状态：它现在保存为本机偏好；手动拖动排序时仍然会自动切回“手动排序”。
- 排序方式不是“手动排序”时，顶部汇总会写明“· 排序：最近修改”，两台电脑顺序看起来不同时一眼能找到原因。
- 论文选项里的“显示全部”顺便把排序方式复位成手动排序。
- 新增三项检查：默认手动排序、非法取值回退、保存后重新加载仍然生效。

## 1.11.0

- 右上角新增临时开关“显示隐藏 N 篇 / 收起隐藏 N 篇”：点一下就能看一眼按当前设置被隐藏的论文，再点一下收回去。只有真有隐藏项时才出现，按阶段分组翻页时不需要它、自动收起。
- 展开出来的这些论文整体压暗、底色换成主题的柔和色，并在标题行挂一个“已隐藏 · 在审”的标记，和正常论文一眼分得开。
- 这个开关只影响此刻显示，不改动“隐藏所选阶段的论文”这个长期设置；论文选项里也加了一句说明指向它。
- 新增两项检查：临时展开只影响本次显示、长期设置保持原样。

## 1.10.0

- 同步事件立了兼容契约：遇到本机不认识的新字段、新阶段编号或改名后的阶段时，只跳过那一条编辑，其余改动照常生效，原文件保留。
- 整条记录来自更高版本时只保留、不应用，等升级到认识它的版本后自动补上；状态栏会写明“N 条记录含更新版本的字段，已保留待升级后生效”，不再和“暂无法读取”混在一起。
- 本机自己写出去的记录仍然严格校验，出现未知字段直接报错，避免把本机都看不懂的内容广播出去。
- 契约写进 AGENTS.md 与 CONTRIBUTING.md，并补了 8 项检查：未知字段计数、同文件可读部分生效、未来版本整条保留、本地日志保留原文、状态提示等。

## 1.9.0

- 新增可选反馈音效，默认关闭。设置 → 视图与分页 → 音效，三档选择：关、只完成时、完成和取消都响。
- 音色三选一：木质（短促的嗒声）、清脆（带泛音，像敲玻璃）、水滴（音高下滑）；音量一个滑杆，另有三个试听按钮分别试完成音、取消音、收录音。
- 勾满七个阶段时换成一小段三音奖励音；同一时间只响一次（150 毫秒节流），连点不会变成机关枪。
- 音效全部在内存里合成，仓库不带任何音频素材；设置只存本机，不随同步跑到别的电脑。
- 修复设置窗口长句不换行、被切在窗口外的问题：横向滚动让内容宽度不受限制，现在每页宽度跟着视口走。
- 开发用导出 `--sound-check <目录>`：把三种音色乘三种场景共九个 WAV 写到指定目录，方便直接试听。

## 1.8.0

- 新增底部提示条：勾选阶段后，如果这篇论文被"暂时隐藏的阶段"藏起来、或被分页挪到别的页，底部会滑出一条说明，告诉你它去哪了，并给出动作按钮。
- 被隐藏时给“立即显示”（把该阶段从隐藏列表里去掉，这类论文一起回到列表）；被分页时给“翻到第 N 页”。八秒自动消失，也可以点 × 收起。
- 归档和“复制为新论文”也会各给一句交代；导入备份保留原来的模态提示。普通的阶段勾选（论文还在原地）不会弹。
- 设置 → 视图与分页 增加“操作后在底部显示提示条”开关，默认开启，只存本机。
- 阶段勾选从监听 Click 改成监听状态变化，键盘、鼠标和自动化切换现在走同一条路径。

## 1.7.0

- 文字分三档：标题（论文标题、产品名）、正文（阶段标签、天数、下一步、按钮、表单）、次要（汇总行、页脚、提示文字）。每一档可以单独选字体、调字号比例（60%–200%）和改颜色。
- 字体和颜色留空就跟随“基础字体”和主题，不会因为没设置而变样；设置窗口自己的标签和控件也跟着正文、次要两档走。
- 设置新增“字体与文字”分类，把基础字体、基础字号、界面缩放、自动放大窗口和三档设置集中起来；“外观”页只留主题、布局、背景图片、主色底色和不透明度。
- 文字颜色带对比度提醒：选定颜色后按卡片底色算对比度，低于 2.5:1 直接告诉你会看不清。
- 颜色值会校验，非法值丢弃并回退到主题色。

## 1.6.2

- 修复 Alt+Tab 里还能看到小部件的问题。任务栏按钮原本靠 `ShowInTaskbar=false` 去掉了，但 Alt+Tab 认的是另一个窗口标志，现在补上 `WS_EX_TOOLWINDOW`，两处都不再出现；你自己打开的设置、论文资料等窗口仍然留在任务栏和 Alt+Tab 里。
- 新增背景图片：设置 → 外观 → 背景图片，可选本机任意图片（png、jpg、jpeg、webp、bmp、gif），铺满小部件并按宽高比裁剪。
- 新增“图片遮罩”滑杆：数值越大，图片被主题底色盖得越多、文字越清楚；和“背景不透明度”配合使用。
- 背景图片只在本机生效，路径不参与同步；图片被移动或删除时会提示找不到，并自动回退到主题底色。

## 1.6.1

- 修复在设置里切换主题后看不清的问题：设置窗口自己的底色和文字原本在打开时就定死了，换成浅色主题后控件变成浅色文字、窗口还是深色底，主题列表几乎无法辨认。现在窗口、标签和说明文字全部实时跟随主题。
- 修复小部件底部状态栏的分隔线在换主题后不更新。
- 主题列表的色点改成每套主题自己的固定颜色，并去掉多余描边，避免被当前主题染色。
- CONTRIBUTING 增加一条界面验收规则：改外观代码后要切换浅色、深色和列表布局各截图检查一次。

## 1.6.0

- 界面整套重做：新增五套排版风格，共 23 套主题。纸感用中性灰白、细进度条、阶段只用圆点加文字；柔光用米白暖色、大圆角和胶囊阶段；夜航是深色玻璃，霜蓝取自 Nord 官方色值；标签按类别给阶段分色；极简配列表布局使用。
- 主题在设置里按风格分组，每行三个色点预览（底色、卡片色、强调色），点一下立刻生效。原来的六套主题保留为“经典 · ”，老用户升级后外观完全不变。
- 新增列表布局：去掉卡片，只用一条分隔线，一屏能看到更多篇论文。
- 新增“跟随 Windows 的浅色/深色设置”，浅色主题会自动换成它的深色搭档。
- 进度条厚度新增“跟随主题”，由主题决定（4 到 20 像素）；原来的 14/20/28 仍然可选。
- 控件样式统一重写：按钮、输入框、下拉框、滑杆、复选框、列表框都跟着主题走，圆角、边框、间距来自同一套尺度。设置、新增论文、论文资料、论文选项、修改记录和小部件现在是同一套语言。
- 百分比从进度条右侧移到标题行右侧，字号不再一律 23 像素强调色：纸感、标签、极简用次要灰，柔光、夜航才用强调色。
- 新增开发用导出 `--theme-gallery <目录>`：用真实界面渲染每一套主题，输出一张主题一览图。

## 1.5.1

- 去掉所有假定用户使用某个特定网盘的说法。程序界面、README、隐私说明和发布文档统一改说“同步文件夹”，任何会自动同步文件的网盘都能用同一套做法。
- 页脚状态和启动等待提示改成中性的“同步文件夹”说法，不再出现任何网盘品牌名。
- 公开文件审计里的私人路径特征保留网盘文件夹名，它是用来拦住泄漏的个人路径的，不是要求用户装哪家网盘。

## 1.5.0

- 设置拆成四个分类，左侧切换：外观、视图与分页、同步与启动、关于与反馈。原来是一整条长列表。
- 字号和界面缩放分成两个独立设置。字号 9–36 只改文字大小；界面缩放 80%–200% 把文字和间距一起缩放。滑杆旁边实时提示当前窗口大约能完整显示几篇论文。
- 新增“调整字号或界面缩放时，自动放大窗口”，默认关闭；打开后窗口按比例跟着长，最多占屏幕工作区的一半。
- 大字号或窄窗口下，标题栏按钮自动换到第二行，不再挤掉“PaperFlow”标题；阶段标签保持文字并允许换行，卡片相应变高。
- 修复大字号下百分比和“下一步”被右边缘裁掉的问题。
- “关于与反馈”页显示产品名、版本号、构建提交号、主页、反馈邮箱和 MIT 许可，主页与邮箱用系统浏览器打开。
- LICENSE 版权行改为 `Copyright (c) 2026 Panwang Yuang`；发布脚本把 Git 提交号写进程序，关于页据此显示构建版本。

## 1.4.1

- 小部件不再出现在任务栏和 Alt+Tab 列表，只在桌面和系统托盘里；程序照常运行，双击托盘图标即可恢复。
- 任务栏既已不再提供恢复入口，标题栏的最小化按钮随之移除，收起到托盘统一由 `×` 负责。
- 设置、论文选项、论文资料、修改记录、新增论文这些由用户自己打开的窗口仍然出现在任务栏，切走了也能找回来。
- 界面文字与文档统一把“挂件”改称“桌面小部件”。

## 1.4.0

- 产品名由 PaperProgress 改为 PaperFlow：程序、启动器、窗口与托盘标题、GitHub 制品名和文档同步改名。
- 首次启动新版会把 `%LOCALAPPDATA%/PaperProgress` 的本机资料复制到 `%LOCALAPPDATA%/PaperFlow`，原目录保持不动；论文、修改记录、同步记录、备份和同步设备标识一并迁移。
- 开机启动项由 `PaperProgress` 改写为 `PaperFlow`，旧值自动清除；旧的启动器仍能读取同一份清单并启动新程序。
- 新增仓库内统一的版本与产品名来源，窗口、设置页和宣传导出不再各自硬编码版本号。

## 1.3.0

- “投稿”阶段显示为“在审”，历史记录的稳定阶段标识和勾选进度继续兼容读取。
- 默认隐藏当前在审论文，可选择更多阶段；按最后一个已勾选阶段归类，进入返修/收录后重新归类。
- 高、中、低优先级改为标题最左侧的三个点：高、中、低分别点亮三点、两点、一点；点击圆点或编辑资料均可修改，支持多选优先级筛选。
- 卡片标题右侧的“论文资料”文字按钮与“···”按钮移除，改为右下角统一的论文设置按钮，集中编辑资料、查看修改记录、上移下移、复制与归档。
- 可按高、中、低分三页，或按所选阶段与其余论文分两页；分页和筛选保存在本机。
- 设置增加论文标题加粗开关，修复夜墨主题未勾选阶段的文字对比度。
- 提供纯虚构数据的高清界面及宣传图导出入口，不读取正式资料或桌面画面。
- 新的优先级同步字段需要各设备升级至 1.3.0 或更新版本。旧版会拒绝包含新字段的事件，请从托盘退出后重新打开同一启动器。

## 1.2.0

- 拖动论文标题或卡片空白处调整优先顺序，显示插入提示并同步排序；筛选中排序保留隐藏论文的位置。
- 标题栏和小部件空白处可拖动窗口，松手后记住位置。
- 左上角图标跟随主题和自定义主色，程序、任务栏及托盘图标保持固定。
- 卡片去掉状态角标与独立资料行；资料入口移至标题右侧，天数和下一步并入阶段区域，缩小间距以显示更多论文。
- 修复 Windows PowerShell 更新已有安装清单时的空路径兼容问题，保留上一个版本清单并检查连续更新。

## 1.1.0

- 为程序、任务栏和托盘提供多尺寸专用图标。
- 使用小号新增按钮，搜索、筛选、排序和紧凑模式集中在论文选项。
- 六种主题、自定义颜色、背景透明度、字体及字号，支持即时预览与取消恢复。
- 不可变事件文件通过同步文件夹交换，多设备按字段合并，离线可编辑。
- 独立启动器、版本目录、SHA-256 校验、本机运行缓存与上次可用版本回退。
- 公开源码与私人资料隔离，Git 提交审计、同一提交生成源码包与程序包、可选私人安装部署。

## 1.0.0

- 初版 Windows 桌面小部件：七阶段进度、多论文、资料编辑、备份、托盘及置顶。
