# Changelog

## 1.8.0

- 新增底部提示条：勾选阶段后，如果这篇论文被"暂时隐藏的阶段"藏起来、或被分页挪到别的页，底部会滑出一条说明，告诉你它去哪了，并给出动作按钮。
- 被隐藏时给“立即显示”（把该阶段从隐藏列表里去掉，这类论文一起回到列表）；被分页时给“翻到第 N 页”。八秒自动消失，也可以点 × 收起。
- 归档和“复制为新论文”也会各给一句交代；导入备份保留原来的模态提示。普通的阶段勾选（论文还在原地）不会弹。
- 设置 → 视图与分页 增加“操作后在底部显示提示条”开关，默认开启，只存本机。
- 阶段勾选从监听 Click 改成监听状态变化，键盘、鼠标和自动化切换现在走同一条路径。

## 1.7.0

- 文字分三档：标题（论文标题、产品名）、正文（阶段标签、天数、下一步、按钮、表单）、次要（汇总行、页脚、提示文字）。每一档可以单独选字体、调字号比例（60%–200%）和改颜色。
- 字体和颜色留空就跟随“基础字体”和主题，不会因为没设置而变样；设置窗口自己的标签和控件也跟着正文、次要两档走。
- 设置新增“字体与文字”分类，把基础字体、基础字号、界面缩放、自动放大窗口和三档设置集中起来；“外观”页只留主题、布局、背景图片、主色底色和不透明度。
- 文字颜色带对比度提醒：选定颜色后按卡片底色算对比度，低于 2.5:1 直接告诉你会看不清。
- 颜色值会校验，非法值丢弃并回退到主题色。

## 1.6.2

- 修复 Alt+Tab 里还能看到小部件的问题。任务栏按钮原本靠 `ShowInTaskbar=false` 去掉了，但 Alt+Tab 认的是另一个窗口标志，现在补上 `WS_EX_TOOLWINDOW`，两处都不再出现；你自己打开的设置、论文资料等窗口仍然留在任务栏和 Alt+Tab 里。
- 新增背景图片：设置 → 外观 → 背景图片，可选本机任意图片（png、jpg、jpeg、webp、bmp、gif），铺满小部件并按宽高比裁剪。
- 新增“图片遮罩”滑杆：数值越大，图片被主题底色盖得越多、文字越清楚；和“背景不透明度”配合使用。
- 背景图片只在本机生效，路径不参与同步；图片被移动或删除时会提示找不到，并自动回退到主题底色。

## 1.6.1

- 修复在设置里切换主题后看不清的问题：设置窗口自己的底色和文字原本在打开时就定死了，换成浅色主题后控件变成浅色文字、窗口还是深色底，主题列表几乎无法辨认。现在窗口、标签和说明文字全部实时跟随主题。
- 修复小部件底部状态栏的分隔线在换主题后不更新。
- 主题列表的色点改成每套主题自己的固定颜色，并去掉多余描边，避免被当前主题染色。
- CONTRIBUTING 增加一条界面验收规则：改外观代码后要切换浅色、深色和列表布局各截图检查一次。

## 1.6.0

- 界面整套重做：新增五套排版风格，共 23 套主题。纸感用中性灰白、细进度条、阶段只用圆点加文字；柔光用米白暖色、大圆角和胶囊阶段；夜航是深色玻璃，霜蓝取自 Nord 官方色值；标签按类别给阶段分色；极简配列表布局使用。
- 主题在设置里按风格分组，每行三个色点预览（底色、卡片色、强调色），点一下立刻生效。原来的六套主题保留为“经典 · ”，老用户升级后外观完全不变。
- 新增列表布局：去掉卡片，只用一条分隔线，一屏能看到更多篇论文。
- 新增“跟随 Windows 的浅色/深色设置”，浅色主题会自动换成它的深色搭档。
- 进度条厚度新增“跟随主题”，由主题决定（4 到 20 像素）；原来的 14/20/28 仍然可选。
- 控件样式统一重写：按钮、输入框、下拉框、滑杆、复选框、列表框都跟着主题走，圆角、边框、间距来自同一套尺度。设置、新增论文、论文资料、论文选项、修改记录和小部件现在是同一套语言。
- 百分比从进度条右侧移到标题行右侧，字号不再一律 23 像素强调色：纸感、标签、极简用次要灰，柔光、夜航才用强调色。
- 新增开发用导出 `--theme-gallery <目录>`：用真实界面渲染每一套主题，输出一张主题一览图。

## 1.5.1

- 去掉所有假定用户使用某个特定网盘的说法。程序界面、README、隐私说明和发布文档统一改说“同步文件夹”，任何会自动同步文件的网盘都能用同一套做法。
- 页脚状态和启动等待提示改成中性的“同步文件夹”说法，不再出现任何网盘品牌名。
- 公开文件审计里的私人路径特征保留网盘文件夹名，它是用来拦住泄漏的个人路径的，不是要求用户装哪家网盘。

## 1.5.0

- 设置拆成四个分类，左侧切换：外观、视图与分页、同步与启动、关于与反馈。原来是一整条长列表。
- 字号和界面缩放分成两个独立设置。字号 9–36 只改文字大小；界面缩放 80%–200% 把文字和间距一起缩放。滑杆旁边实时提示当前窗口大约能完整显示几篇论文。
- 新增“调整字号或界面缩放时，自动放大窗口”，默认关闭；打开后窗口按比例跟着长，最多占屏幕工作区的一半。
- 大字号或窄窗口下，标题栏按钮自动换到第二行，不再挤掉“PaperFlow”标题；阶段标签保持文字并允许换行，卡片相应变高。
- 修复大字号下百分比和“下一步”被右边缘裁掉的问题。
- “关于与反馈”页显示产品名、版本号、构建提交号、主页、反馈邮箱和 MIT 许可，主页与邮箱用系统浏览器打开。
- LICENSE 版权行改为 `Copyright (c) 2026 Panwang Yuang`；发布脚本把 Git 提交号写进程序，关于页据此显示构建版本。

## 1.4.1

- 小部件不再出现在任务栏和 Alt+Tab 列表，只在桌面和系统托盘里；程序照常运行，双击托盘图标即可恢复。
- 任务栏既已不再提供恢复入口，标题栏的最小化按钮随之移除，收起到托盘统一由 `×` 负责。
- 设置、论文选项、论文资料、修改记录、新增论文这些由用户自己打开的窗口仍然出现在任务栏，切走了也能找回来。
- 界面文字与文档统一把“挂件”改称“桌面小部件”。

## 1.4.0

- 产品名由 PaperProgress 改为 PaperFlow：程序、启动器、窗口与托盘标题、GitHub 制品名和文档同步改名。
- 首次启动新版会把 `%LOCALAPPDATA%/PaperProgress` 的本机资料复制到 `%LOCALAPPDATA%/PaperFlow`，原目录保持不动；论文、修改记录、同步记录、备份和同步设备标识一并迁移。
- 开机启动项由 `PaperProgress` 改写为 `PaperFlow`，旧值自动清除；旧的启动器仍能读取同一份清单并启动新程序。
- 新增仓库内统一的版本与产品名来源，窗口、设置页和宣传导出不再各自硬编码版本号。

## 1.3.0

- “投稿”阶段显示为“在审”，历史记录的稳定阶段标识和勾选进度继续兼容读取。
- 默认隐藏当前在审论文，可选择更多阶段；按最后一个已勾选阶段归类，进入返修/收录后重新归类。
- 高、中、低优先级改为标题最左侧的三个点：高、中、低分别点亮三点、两点、一点；点击圆点或编辑资料均可修改，支持多选优先级筛选。
- 卡片标题右侧的“论文资料”文字按钮与“···”按钮移除，改为右下角统一的论文设置按钮，集中编辑资料、查看修改记录、上移下移、复制与归档。
- 可按高、中、低分三页，或按所选阶段与其余论文分两页；分页和筛选保存在本机。
- 设置增加论文标题加粗开关，修复夜墨主题未勾选阶段的文字对比度。
- 提供纯虚构数据的高清界面及宣传图导出入口，不读取正式资料或桌面画面。
- 新的优先级同步字段需要各设备升级至 1.3.0 或更新版本。旧版会拒绝包含新字段的事件，请从托盘退出后重新打开同一启动器。

## 1.2.0

- 拖动论文标题或卡片空白处调整优先顺序，显示插入提示并同步排序；筛选中排序保留隐藏论文的位置。
- 标题栏和小部件空白处可拖动窗口，松手后记住位置。
- 左上角图标跟随主题和自定义主色，程序、任务栏及托盘图标保持固定。
- 卡片去掉状态角标与独立资料行；资料入口移至标题右侧，天数和下一步并入阶段区域，缩小间距以显示更多论文。
- 修复 Windows PowerShell 更新已有安装清单时的空路径兼容问题，保留上一个版本清单并检查连续更新。

## 1.1.0

- 为程序、任务栏和托盘提供多尺寸专用图标。
- 使用小号新增按钮，搜索、筛选、排序和紧凑模式集中在论文选项。
- 六种主题、自定义颜色、背景透明度、字体及字号，支持即时预览与取消恢复。
- 不可变事件文件通过同步文件夹交换，多设备按字段合并，离线可编辑。
- 独立启动器、版本目录、SHA-256 校验、本机运行缓存与上次可用版本回退。
- 公开源码与私人资料隔离，Git 提交审计、同一提交生成源码包与程序包、可选私人安装部署。

## 1.0.0

- 初版 Windows 桌面小部件：七阶段进度、多论文、资料编辑、备份、托盘及置顶。
