# Contributing

请使用独立 Git clone 和合成论文复现问题。不要在 Issue、截图、补丁、日志或附件中放入真实论文、作者、合作者、未公开摘要、同步路径或个人 JSON。

1. 在独立开发目录复现，修复源码并为数据、同步或更新方面的问题补充有意义的回归检查。
2. 执行测试和构建；界面改动需实际检查外观、点击和设置保存。
   改外观或主题相关的代码时，除了看默认主题，还要在设置里**切换**主题后再截图检查：浅色主题、深色主题、列表布局各一次，设置窗口和小部件都要看清。曾经出过一次事故——设置窗口自己的底色是在建窗口时定死的，换到浅色主题后窗口仍是深色，主题列表几乎看不清。
3. 安装提交钩子：`git config core.hooksPath .githooks`。提交前看完整暂存差异，运行 `scripts/Test-PublicTree.ps1 -Staged`。
4. 修改 `PaperFlow/PaperFlow.csproj` 中版本号，更新 CHANGELOG，再提交。已部署版本不能覆盖为不同内容。
5. 使用 [发布流程](docs/RELEASING.md)。公开版本和自用版本来自同一源码，不维护私人代码分支。

### 同步格式的兼容契约

改动同步事件（`SyncEngine.cs`）时必须守住这几条，它们保护的是"不同电脑装了不同版本"这种情况：

1. 新增字段一律可选、带默认值，旧数据和新数据都能读。
2. 读到不认识的字段、阶段编号或阶段名时，跳过这一条编辑、保留原文件、计数并在状态里提示，**绝不整条拒收**——一条记录里往往装着同一次操作改的多个字段，整条拒收会连带丢掉能读懂的部分。
3. 整条记录版本号更高的，原样保留在本地日志里、先不应用，等升级后自动补上。
4. 本机自己写出去的记录仍走严格校验：出现未知字段直接报错，免得把看不懂的内容广播给别人。
5. 改完要补检查：未知字段计数、同一文件里可读部分照常生效、未来版本整条保留、本地日志保留原文、状态提示。

CI 只验证和生成制品；创建远程仓库、推送和发布 Release 由维护者执行。不得把个人安装文件夹作为源码 PR。
